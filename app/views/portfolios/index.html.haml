
.whiteblockportfolio
  .port-top
    %h1 Портфолио
  .portfoliosblocks
    - @portfolio.each_slice(1) do |pos|

      - pos.each do |po|
        .portfolio_block
          .portfolio_image_wrapper{:style=>"display: inline-block"}
            = link_to po do
              = image_tag po.image.url(:medium), class: "portfolio_block_img"
          .portf_title
            %h1= link_to po.title, po
          .portf_text{:style=>"display: inline-block"}
            %p= truncate(po.description1, length: 150)
          .port_button
            = link_to "ПОДРОБНЕЕ", po,class: "port_button_block"
            - if user_signed_in?
              = link_to "Edit", edit_portfolio_path(po), class: "btn btn-default"
    .portfolio_block
      - if user_signed_in?
        = link_to "НОВЫЙ ПРОЕКТ", new_portfolio_path, class: "btn btn-default"

= render "layouts/bottomnavigation"